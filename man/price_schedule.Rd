% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classroom.R
\name{price_schedule}
\alias{price_schedule}
\alias{schedule_cost}
\alias{schedule_quantity}
\title{A unit price that changes with the quantity bought}
\usage{
price_schedule(price, discount = NULL, after = NULL, breaks = NULL)

schedule_cost(schedule, q)

schedule_quantity(schedule, spend)
}
\arguments{
\item{price}{Base unit price, or a vector of tier prices when \code{breaks} is
given.}

\item{discount}{Fraction off every unit after the first \code{after} units.}

\item{after}{Units bought at the base price before the discount applies.}

\item{breaks}{Upper bound of each tier except the last, which is
unbounded. Must be increasing.}

\item{schedule}{A \code{price_schedule}, or a plain price.}

\item{q}{Quantities.}

\item{spend}{Money available.}
}
\value{
An object of class \code{price_schedule}.

A numeric vector the length of \code{q}.
}
\description{
Quantity discounts ("25\% off after the second"), block tariffs and
rationing all make a good's price depend on how much of it you already
have. A price schedule holds the unit price in each tier, and \code{\link[=budget]{budget()}}
accepts one in place of a number, giving a \emph{kinked} budget line.
}
\details{
Two ways to build one: the common case as a base price with a discount
after a threshold, or the general case as a vector of tier prices and the
upper bounds of all but the last tier.

A kinked budget changes what the downstream functions can assume:
\code{\link[=budget_line]{budget_line()}} traces the frontier through the kinks, \code{\link[=geom_budget]{geom_budget()}}
draws it as a path, and \code{\link[=optimal_bundle]{optimal_bundle()}} searches along the frontier
numerically instead of using a closed form, since the tangency condition
no longer identifies the optimum on its own (the kink itself is a
candidate). The marginal rate of substitution is still available from
\code{\link[=mrs]{mrs()}}, but the budget's \code{slope} is \code{NA}.
}
\examples{
# Paintings $20 each, 25\% off after the second
paintings <- price_schedule(20, discount = 0.25, after = 2)
paintings
schedule_cost(paintings, 0:4)      # cumulative cost of 0..4 paintings
schedule_quantity(paintings, 100)  # how many $100 buys

# The same thing as tiers
price_schedule(c(20, 15), breaks = 2)

b <- budget(income = 100, px = 10, py = paintings)
b
budget_line(b, n_points = 5)
}
