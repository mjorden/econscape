% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/micro.R
\name{budget}
\alias{budget}
\title{A budget constraint}
\usage{
budget(income, px, py)
}
\arguments{
\item{income}{Income, or total outlay. Positive.}

\item{px, py}{Prices of \code{x} and \code{y}: a positive number, or a
\code{\link[=price_schedule]{price_schedule()}} for a good whose unit price changes with the quantity
bought (quantity discounts, block tariffs). A schedule makes the budget
line kinked; see \code{\link[=price_schedule]{price_schedule()}} for what that changes.}
}
\value{
A list of class \code{budget} with the inputs plus the derived
\code{x_max} and \code{y_max} (the most of each good the income buys on its own),
\code{slope} (\code{-px / py}, or \code{NA} when a price is a schedule), \code{kinked}
(\code{TRUE} when either price is a schedule) and the cost functions \code{cost_x}
and \code{cost_y}. Because it carries functions, two budgets built from the
same arguments are equal under \code{\link[=all.equal]{all.equal()}} but not \code{\link[=identical]{identical()}};
compare the fields if you need a strict test.
}
\description{
Describes the set of bundles affordable at income \code{income} when the two
goods cost \code{px} and \code{py}: every \eqn{(x, y)} with
\eqn{p_x x + p_y y \le I}. For a producer the same object is an isocost
line, with \code{income} read as total outlay and the prices as input prices.
}
\examples{
b <- budget(income = 100, px = 2, py = 5)
b
b$x_max
}
\seealso{
\code{\link[=budget_line]{budget_line()}} to get plottable coordinates, \code{\link[=optimal_bundle]{optimal_bundle()}}
to solve against a utility function.
}
